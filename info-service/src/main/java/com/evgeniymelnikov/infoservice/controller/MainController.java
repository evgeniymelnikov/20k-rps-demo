package com.evgeniymelnikov.infoservice.controller;

import org.springframework.stereotype.Controller;

@Controller
public class MainController {

//    @PostMapping("/perform_login")
//    @ResponseBody
//    public String login(@RequestBody UserLoginRequestInfo loginInfo) {
//        if (StringUtils.hasText(loginInfo.getUsername()) ||  StringUtils.hasText(loginInfo.getPassword())) {
//            throw new IllegalArgumentException("Пустое имя пользователя или пароль.");
//        }
//
//
//    }
}
